--- a/frontend/index.html
+++ b/frontend/index.html
@@ -6342,10 +6342,16 @@ function loadMarker(markerId) {
     // Update UI state to track selected marker
     UIState.selectedMarkerId = markerId;
 
     // Show marker metadata panel
-    document.getElementById("marker-meta").classList.remove("hidden");
+    const markerPanel = document.getElementById("marker-meta");
+    markerPanel.classList.remove("hidden");
+    
+    // Scroll panel into view within the right panel without affecting main viewport
+    setTimeout(() => {
+        markerPanel.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
+    }, 50);
 
     document.getElementById("marker-name-input").value = marker.name;
     document.getElementById("marker-description-input").value = marker.description || "";
@@ -6127,11 +6133,20 @@ function deleteUnplacedMarker(markerId, mapId) {
     const mapData = worldData.maps[mapId];
     if (!mapData || !mapData.markers) return;
     
+    if (!confirm("Delete this marker?")) return;
+    
     const index = mapData.markers.findIndex(m => m.id === markerId);
     if (index !== -1) {
         mapData.markers.splice(index, 1);
+        
+        // Clear selected marker if it was deleted
+        if (UIState.selectedMarkerId === markerId) {
+            UIState.selectedMarkerId = null;
+        }
+        
         scheduleVaultSave();
-        renderUnplacedMarkers(mapId);
+        renderUIState();  // Refresh the entire map view to remove marker from map
     }
 }
